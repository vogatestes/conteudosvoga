{
  "name": "content-studio-backend",
  "version": "1.0.0",
  "description": "Backend que fala com a API da Anthropic (Claude) para o app de conteúdo — mantém a API key segura, fora do site estático.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
