{
  "name": "content-studio-backend",
  "version": "2.0.0",
  "description": "Backend que fala com a API gratuita do Google Gemini para o app de conteúdo — mantém a API key segura, fora do site estático.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
